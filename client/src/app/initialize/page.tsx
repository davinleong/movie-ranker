import InitializeClient from "./InitializeClient";

export default async function Initialize() {
  return (
    <>
      <title>Initialize</title>
      <meta name="description" content="Initializing a new account" />
        <InitializeClient />
    </>
  );
}
